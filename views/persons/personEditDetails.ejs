<!DOCTYPE html>
<html>
  <head>
    <title>Person Detail</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <div class="container">
      <h1>Person Detail</h1>
      <form action="/users/edit/<%=item._id%>" method="post">
        <table>
          <tbody>
            <tr>
              <td>Id</td>
              <!-- Como o id não pode ser alterado usamos o input hidden para o enviar como campo do formulário. -->
              <td>
                <input
                  type="hidden"
                  name="_id"
                  id="_id"
                  value="<%= item._id %>"
                /><%= item._id %>
              </td>
            </tr>
            <tr>
              <td>Name</td>
              <td>
                <input
                  type="text"
                  name="name"
                  id="name"
                  value="<%= item.name %>"
                />
              </td>
            </tr>
            <tr>
              <td>Phone number</td>
              <td>
                <input
                  type="text"
                  name="details"
                  id="details"
                  value="<%= item.phonenumber %>"
                />
              </td>
            </tr>
            <tr>
              <td>Email</td>
              <td>
                <input
                  type="text"
                  name="details"
                  id="details"
                  value="<%= item.email %>"
                />
              </td>
            </tr>
            <tr>
              <td colspan="2"><button type="submit">Edit</button></td>
            </tr>
          </tbody>
        </table>
      </form>
    </div>
    <h3><a href="/users">Watch all the persons</a></h3>
  </body>
</html>
